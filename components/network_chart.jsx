/* eslint-disable react/prop-types */

const specs = {
  margin: {
    top: 30,
    right: 25,
    bottom: 70,
    left: 50
  }
};

const NetworkChart = ({ width, height, data }) => {

  return ();
};

export default NetworkChart;